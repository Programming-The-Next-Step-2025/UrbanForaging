% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_foraging_location.R, R/week-2_functions.R
\name{add_foraging_location}
\alias{add_foraging_location}
\title{Add a New Foraging Location}
\usage{
add_foraging_location(data, plant, lat, lon, month, notes = "")

add_foraging_location(data, plant, lat, lon, month, notes = "")
}
\arguments{
\item{data}{A data frame of existing foraging locations.}

\item{plant}{Name of the plant found (character).}

\item{lat}{Latitude of the location (numeric).}

\item{lon}{Longitude of the location (numeric).}

\item{month}{The month when the plant is harvestable (e.g., "April").}

\item{notes}{Optional notes about the location or plant (character).}
}
\value{
A data frame with the new location added.

A data frame with the new location added.
}
\description{
Adds a new plant observation to an existing foraging dataset. Useful for user-contributed entries in the Urban Foraging Map app.

Adds a new plant observation to an existing foraging dataset. Useful for user-contributed entries in the Urban Foraging Map app.
}
\details{
The new entry is appended as a row to the input data frame using `dplyr::bind_rows()`.

The new entry is appended as a row to the input data frame using `dplyr::bind_rows()`.
}
\examples{
df <- data.frame(
  plant = character(),
  lat = numeric(),
  lon = numeric(),
  month = character(),
  notes = character(),
  stringsAsFactors = FALSE
)
df <- add_foraging_location(df, "Wild Garlic", 52.37, 4.89, "March", "Under the bridge")

df <- data.frame(
  plant = character(),
  lat = numeric(),
  lon = numeric(),
  month = character(),
  notes = character(),
  stringsAsFactors = FALSE
)
df <- add_foraging_location(df, "Wild Garlic", 52.37, 4.89, "March", "Under the bridge")

}
